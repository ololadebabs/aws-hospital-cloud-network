#This tells Terraform which provider it must download. Provider blocks allow Terraform to communicate with external platforms such as AWS
terraform {
  required_version = ">= 1.5.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 6.0"
    }
  }
}

